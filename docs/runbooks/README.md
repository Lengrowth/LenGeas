# Development and deferred production runbooks

The Phase 01 server runbooks remain active for the ADR-0011 development host. `development-host-backup.md` and `development-host-incident.md` define the current single-host backup/recovery boundary and incident response. The state, origin, credential, AZ, failover, and regional documents here describe deferred production operations. They require an activated staging or isolated synthetic environment before any scenario can be recorded as passed. Their presence is not live evidence.
