# Account deletion and privacy rights

Rights requests are audited workflows. Deletion revokes sessions, revokes provider identities, schedules identifier removal, creates a non-authenticating tombstone, and pseudonymizes retained financial history. Legal hold blocks deletion. Evidence stores request IDs and stable operation codes only, never raw tokens or PII.
