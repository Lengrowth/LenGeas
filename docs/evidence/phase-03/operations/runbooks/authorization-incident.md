# Authorization incident

Fail closed on tenant escape, role escalation, forged scope, support-grant misuse, or service-account overreach. Revoke the affected session, service client, support grant, or AI credential; preserve request/audit IDs without authorization headers; rerun the negative suites; and inspect that every persistence query carries the trusted scope. The incident closes only with zero unresolved high or critical findings.
