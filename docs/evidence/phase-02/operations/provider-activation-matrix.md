# Phase 02 provider activation matrix

This redacted matrix is the current provider-readiness register for the
development-first Phase 02 scope. It contains no credentials, secret values,
account identifiers, customer data, or claims of live production activation.
Production activation requires a later rollout ADR and fresh capability
inventory.

| provider | phase02_state | plan_capabilities | expected_data | migration_boundary | activation_owner_decision | credentials/evidence |
|---|---|---|---|---|---|---|
| AWS account and control plane | Development account is inventoried; Organization, Control Tower, managed Phase 02 estate, and budgets are deferred. | Current IAM identity supports limited EC2 inventory; Organizations and budget views are unavailable. Target capabilities include Organizations, CloudTrail, Config, GuardDuty, Security Hub, ECS, managed data, and observability. | Current host metadata, synthetic Compose telemetry, and future audit/security/resource metadata. | Keep the existing `us-east-1a` development host and its synthetic data. Do not migrate it into the future production estate. | Owner accepted development-first activation; a future rollout ADR must approve account structure, regions, capacity, data residency, cost, migration, and rollback. | Read-only inventory in `aws-inventory-2026-09-13.md`; no credentials or state recorded. |
| Cloudflare | Existing `games.lengrowth.com` DNS is authoritative and DNS-only; production edge activation is deferred. | Current target-zone plan, WAF/bot/Turnstile/Access entitlements, Workers/R2 bindings, logging, and origin-auth capabilities are not inventoried. | DNS/TLS/rules, scrubbed edge logs, Worker events, and R2 objects only after rollout activation. | Preserve the direct development endpoint until a separately verified edge cutover; do not expose the origin name as production. | No new paid edge activation in Phase 02. Owner must approve the rollout ADR and actual plan capabilities before proxying or enabling paid controls. | No deployment token stored; limitations recorded in `unavailable-artifacts.md`. |
| MongoDB Atlas | Deferred; no project or cluster is active for Phase 02. | Dedicated production cluster, backups/PITR, PrivateLink, and regional restore remain target capabilities; current Atlas access is unavailable. | Future tenant-scoped documents, indexes, backups, and synthetic rollout fixtures. | No current development data migration. Production data enters only after isolated restore and qualification evidence. | Owner must approve the rollout ADR, Atlas plan/region, capacity, private connectivity, backup/restore, and migration boundary. | No Atlas credentials or connection strings recorded. |
| Supabase | Deferred; no Auth project is created for Phase 02. | Asymmetric signing, JWKS, provider sign-in, environment isolation, and webhook/API capabilities require a future capability inventory. | Future non-secret identity metadata and authentication events; secret values remain external. | No current development identity migration; only synthetic accounts after rollout approval. | Owner must approve the rollout ADR, plan, project separation, signing-key, data-residency, and migration decisions. | No access token, service-role key, or user data recorded. |
| Resend | Deferred; no domain or webhook is configured for Phase 02. | Basic-plan domain verification, sending limits, webhook signing, and retention require provider access and inventory. | Future transactional email metadata and delivery/webhook events; message content and keys remain outside evidence. | No current development email migration; a later rollout must define domain and webhook cutover/rollback. | Owner must approve the rollout ADR and basic-plan domain, DNS, webhook-authenticity, and retention decisions. | No API key or webhook secret recorded. |
| Sentry | Deferred; no project or alert route is active for Phase 02. | Event ingestion, PII scrubbing, alert routing, retention, and export capabilities require a future inventory. | Future scrubbed exception and performance events only. | No current development incident history migration; structured logs remain the fallback. | Owner must approve the rollout ADR, data-processing review, retention, alert ownership, and migration/export plan. | No DSN or auth token recorded. |
| GitHub Actions/OIDC | Repository CI is active; cloud deployment identity is deferred. | Required future capability is restricted plan/apply OIDC with repository, branch, environment, and audience claims. | Build metadata, test results, Terraform plans, and provenance; never provider secrets or state. | CI remains independent of the current host deployment; future rollout adopts the reviewed OIDC roles. | Owner must approve the bootstrap role trust and protected-environment workflow at production activation. | PR checks are recorded in `verification-records.ndjson`; no long-lived cloud credential is stored. |

## Current decision

Phase 02 activates only the existing synthetic development foundation. Every
other provider row is a deferred activation point, not a claim that the
provider is configured or that its plan is available.
