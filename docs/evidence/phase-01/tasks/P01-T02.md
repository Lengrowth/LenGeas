# P01-T02 — Pin toolchains

## Status

`in_review`

## Contract

- Owner role: `infrastructure_owner`
- Reviewer role: `release_manager`
- Depends on: P01-T01
- Artifacts: `.tool-versions`, `pyproject.toml`, `package.json`, `pnpm-lock.yaml`, `uv.lock`, version report.

## Verification

| Command | Result |
|---|---|
| `pnpm install --frozen-lockfile --ignore-scripts` | exit 0 |
| `uv lock --check` | exit 0 |
| `pnpm run version-report` | exit 0 |
| `uv run --frozen ruff check` | exit 0 |
| `uv run --frozen mypy` | exit 0 |

Commit: `e302bcf5e9097e2f3ee77f0ea3044d06889940cb`.

