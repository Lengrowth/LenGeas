# P01-T08 — Protect repository

## Status

`blocked`

## Contract

- Owner role: `infrastructure_owner`
- Reviewer role: `platform_technical_lead`
- Depends on: repository/remote capabilities
- Artifacts: CODEOWNERS, PR template, Dependabot, repository policy export, signed-tag and evidence-link policies.

## Verification

YAML, local links, diff, and no-invented-handle checks passed. The private GitHub remote is confirmed and the owner has admin permission; the live branch read reports `main` is not protected and required status-check enforcement is off. The branch-protection endpoint is unavailable to the active integration (`403`), the ruleset endpoint reports the private-repository plan restriction, and the observed push verification run `34752110824` ended in `startup_failure`. No live protection export, signed tag, or screenshot is claimed.

Commit: `a3e1771`.
