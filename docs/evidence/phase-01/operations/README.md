# Phase 01 operations evidence

The local service map, startup/shutdown commands, and failure behavior are documented in the Compose and local-stack implementation. Docker remains intentionally unavailable on the Windows workstation. Runtime evidence was produced on the authorized AWS Compose host and the GitHub-hosted Ubuntu runner; see `server-runtime.json` and `../commands.ndjson`.
